<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shruti.saveyoursouls">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mainmenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".tutorials" />
        <activity android:name=".helpline" />
        <activity android:name=".flood" />
        <activity android:name=".lightning" />
        <activity android:name=".fire" />
        <activity android:name=".mining" />
        <activity android:name=".earthquake" />
        <activity android:name=".ranchi" />
        <activity android:name=".giridih" />
        <activity android:name=".bokaro" />
        <activity android:name=".purbi" />
        <activity android:name=".dhanbad"></activity>
    </application>

</manifest>